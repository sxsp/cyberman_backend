# Server1 控制面（账号/人设/记忆/密钥）环境变量
# 复制为 .env 并按需修改；未设置的项走代码内默认值。

# 数据目录（SQLite、自动生成的密钥文件都放这里）
SERVER1_DATA_DIR=data
# 数据库文件路径（默认 = $SERVER1_DATA_DIR/server1.db）
# SERVER1_DB_PATH=data/server1.db

# 生产环境务必显式设置；留空则首次启动自动生成并持久化到 data/ 下
# SERVER1_JWT_SECRET=
# SERVER1_FERNET_KEY=

# 访问令牌有效期（分钟，默认 7 天）
SERVER1_TOKEN_EXPIRE_MINUTES=10080
